## Agri Mitra Backend

### Run

1. `npm install`
2. Create database `Agrimitra` in PostgreSQL.
3. Run SQL schema: `psql -U postgres -d Agrimitra -f schema.sql`
4. `node server.js`

Server runs at `http://localhost:5000`.
